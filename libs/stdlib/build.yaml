project:
  name: "utopia"
  version: "0.1.0"

build:
  target: library
  output_dir: "build/lib"
  include_dirs:
    - "."
  sources:
    - "lib/memory.utp"
    - "lib/ffi.utp"
    - "lib/io.utp"
  linker_flags:
    - "-ldl"